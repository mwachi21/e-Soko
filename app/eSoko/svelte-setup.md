## Initialisation

```bash
npx degit sveltejs/template eSoko
```

to automatically build and reload the Svelte frontend when it's changed.
```bash
cd eSoko;npm install;npm run autobuild
```

To run optimised version of the build
```bash
npm run build
```